# Core dependencies
numpy>=1.24.0
opencv-python>=4.8.0
Pillow>=10.0.0
scipy>=1.11.0

# OCR (仅保留 Qwen-OCR)
dashscope>=1.22.2  # 阿里云百炼 Qwen-OCR

# Translation (仅保留 DeepL，使用 REST API)
requests>=2.31.0

# Image processing
scikit-image>=0.21.0
imutils>=0.5.4

# Text detection (craft-text-detector 单独安装，避免 opencv 版本冲突)
# craft-text-detector>=0.4.3

# API and web framework
fastapi>=0.104.0
uvicorn>=0.24.0
python-multipart>=0.0.6
pydantic>=2.5.0

# Utilities
pyyaml>=6.0
python-dotenv>=1.0.0
tqdm>=4.66.0
loguru>=0.7.0

# Font handling
fonttools>=4.44.0
